import React from 'react';
import { SafeAreaView } from 'react-native';

import AudioList from './AudioList';

const App = () => {
  return (
    <SafeAreaView>
      <AudioList />
    </SafeAreaView>
  );
};

export default App;
